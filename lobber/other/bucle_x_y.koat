# Ejemplo de bucle acelerado que relaciona parámetros

(GOAL COMPLEXITY)
(STARTTERM (FUNCTIONSYMBOLS f0))
(VAR x y)
(RULES

f0(x,y) -> f(x,y)

f(x,y) -> f(x-1,y-1)   :|: y > 0
f(x,y) -> fin(x,y)     :|: y <= 0

)

# La aceleración de f->f produce costes y actualizaciones dependientes de 'y'
#   f -> f : x'=x-y, y'=0, [ y>=0 ], cost: y


