# Example in page 67 of TACAS20 (T_ev-dec)

# Can we obtain something better thatn MF=0?

(GOAL COMPLEXITY)
(STARTTERM (FUNCTIONSYMBOLS f0))
(VAR x y)
(RULES

f0(x, y) -> f(x, y)

f(x, y) -> f(x+y, y-1)  :|: x > 0

f(x, y) -> fexit(x, y) :|: x <= 0

)
