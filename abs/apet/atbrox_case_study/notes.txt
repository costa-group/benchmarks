Issues:

- inter_table must be set to no in Atbrox7.abs. It is related to the
  use of sync calls. Ask Miguel so that at least it works.

apet(['apet/atbrox_case_study/Atbrox_5.abs'],[main],[],[cog_select = sound_stable,sched = nondet, mode = syco]).

run_all([indep_approx = fields, trace = no]).


Results:
--------

-- 2 maps, 2 reduces and 2 machines

?- run_all([indep_approx = actor,cog_select = sound_all]).
10428 executions in 4620 ms.

?- run_all([indep_approx = actor]).
456 executions in 247 ms.

?- run_all([indep_approx = fields]).
45 executions in 61 ms.

?- run_all([indep_approx = exact]).
17 executions in 45 ms.

-----------------------------------

-- 2 maps, 2 reduces and 1 machine

?- run_all([indep_approx = actor,cog_select = sound_all]).
36 executions in 30 ms.

?- run_all([indep_approx = actor]).
18 executions in 22 ms.

?- run_all([indep_approx = fields]).
8 executions in 17 ms.

?- run_all([indep_approx = exact]).
8 executions in 16 ms.

-----------------------------------

-- 2 maps, 3 reduces and 2 machines

?- run_all([indep_approx = actor,cog_select = sound_all,timeout=60]).
113651 executions in 55684 ms.

?- run_all([indep_approx = actor,cog_select = sound_stable,timeout=60]).
2008 executions in 1260 ms.

?- run_all([indep_approx = fields]).
180 executions in 210 ms.

?- run_all([indep_approx = exact]).
61 executions in 144 ms.

-----------------------------------

-- 3 maps, 3 reduces and 2 machines

?- run_all([indep_approx = actor,cog_select = sound_all,timeout=60]).
aPET timeout reached (60 secs.): Try setting lower limits or a higher timeout

?- run_all([indep_approx = actor]).
12048 executions in 8064 ms.

?- run_all([indep_approx = fields]).
900 executions in 1122 ms.

?- run_all([indep_approx = exact]).
237 executions in 595 ms.

-----------------------------------

-- 3 maps, 3 reduces and 3 machines

?- run_all([indep_approx = actor,cog_select = sound_all,timeout = 60]).
aPET timeout reached (60 secs.): Try setting lower limits or a higher timeout

?- run_all([indep_approx = actor,timeout = 60]).
aPET timeout reached (60 secs.): Try setting lower limits or a higher timeout

?- run_all([indep_approx = fields]).
2916 executions in 3849 ms.

?- run_all([indep_approx = exact]).
324 executions in 1508 ms.

-----------------------------------

-- 3 maps, 3 reduces and 4 machines

?- run_all([indep_approx = fields]).
3192 executions in 4879 ms.

?- run_all([indep_approx = exact]).
324 executions in 1893 ms.

-----------------------------------

-- 3 maps, 4 reduces and 3 machines

?- run_all([indep_approx = fields]).
19845 executions in 26028 ms.

?- run_all([indep_approx = exact]).
1715 executions in 7628 ms.

-----------------------------------

-- 3 maps, 4 reduces and 4 machines

?- run_all([indep_approx = fields]).
aPET timeout reached (60 secs.): Try setting lower limits or a higher timeout

?- run_all([indep_approx = exact]).
1782 executions in 12517 ms.


-----------------------------------
-----------------------------------

With Atbrox7.abs

apet(['apet/atbrox_case_study/Atbrox7.abs'],[main],[],[cog_select = sound_stable,sched = nondet, mode = syco]).

-- 2 maps, 2 reduces and 1 machine

?- run_all([indep_approx = actor, cog_select = sound_all, inter_table=no]).
54 executions in 51 ms.
true.

?- run_all([indep_approx = actor, cog_select = sound_stable, inter_table=no]).
24 executions in 27 ms.
true.

?- run_all([indep_approx = fields, cog_select = sound_stable, inter_table=no]).
8 executions in 17 ms.
true.

?- run_all([indep_approx = exact, cog_select = sound_stable, inter_table=no]).
8 executions in 19 ms.
true.

-----------------------------------

-- 2 maps, 2 reduces and 2 machines

?- run_all([indep_approx = actor, cog_select = sound_all, inter_table=no]).
11208 executions in 5884 ms.

?- run_all([indep_approx = actor, cog_select = sound_stable, inter_table=no]).
520 executions in 340 ms.

?- run_all([indep_approx = fields, cog_select = sound_stable, inter_table=no]).
75 executions in 106 ms.

?- run_all([indep_approx = exact, cog_select = sound_stable, inter_table=no]).
22 executions in 74 ms.

-----------------------------------

-- 2 maps, 3 reduces and 2 machines

?- run_all([indep_approx = actor,cog_select = sound_all,timeout=60]).
aPET timeout reached (60 secs.): Try setting lower limits or a higher timeout

?- run_all([indep_approx = actor, cog_select = sound_stable, inter_table=no]).
3120 executions in 2203 ms.

?- run_all([indep_approx = fields]).
375 executions in 524 ms.

?- run_all([indep_approx = exact]).
86 executions in 290 ms.

-----------------------------------

-- 3 maps, 3 reduces and 2 machines

?- run_all([indep_approx = actor,cog_select = sound_all,timeout=60]).
aPET timeout reached (60 secs.): Try setting lower limits or a higher timeout

?- run_all([indep_approx = actor]).
18720 executions in 13020 ms.

?- run_all([indep_approx = fields]).
1875 executions in 2673 ms.

?- run_all([indep_approx = exact]).
342 executions in 1171 ms.

-----------------------------------

-- 3 maps, 3 reduces and 3 machines

?- run_all([indep_approx = actor,cog_select = sound_all,timeout = 60]).
aPET timeout reached (60 secs.): Try setting lower limits or a higher timeout

?- run_all([indep_approx = actor,timeout = 60]).
aPET timeout reached (60 secs.): Try setting lower limits or a higher timeout

?- run_all([indep_approx = fields]).
7965 executions in 10630 ms.

?- run_all([indep_approx = exact]).
413 executions in 2871 ms.

-----------------------------------

-- 3 maps, 3 reduces and 4 machines

?- run_all([indep_approx = fields]).
12075 executions in 15679 ms.

?- run_all([indep_approx = exact]).
259 executions in 2982 ms.

-----------------------------------

-- 3 maps, 4 reduces and 3 machines

?- run_all([indep_approx = fields]).
aPET timeout reached (60 secs.): Try setting lower limits or a higher timeout

?- run_all([indep_approx = exact]).
2381 executions in 17516 ms.

-----------------------------------

-- 3 maps, 4 reduces and 4 machines

?- run_all([indep_approx = fields]).
aPET timeout reached (60 secs.): Try setting lower limits or a higher timeout

?- run_all([indep_approx = exact]).
1551 executions in 22155 ms.

